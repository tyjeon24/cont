{
  "name": "cont",
  "version": "0.2.0",
  "description": "Tools for LLM",
  "author": {
    "name": "tyjeon24"
  },
  "mcpServers": {
    "jenkins": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/jenkins.mjs"],
      "env": {
        "JENKINS_URL": "${JENKINS_URL}",
        "JENKINS_USER": "${JENKINS_USER}",
        "JENKINS_TOKEN": "${JENKINS_TOKEN}"
      }
    }
  }
}
